{
  "$id": "match_decision.schema.json",
  "type": "object",
  "required": ["match_id", "decision"],
  "properties": {
    "match_id": {
      "$ref": "../common/uuid.schema.json"
    },
    "decision": {
      "type": "string",
      "enum": ["continue", "requeue", "block"]
    }
  }
}
